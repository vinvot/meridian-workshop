{
  "name": "meridian-e2e-tests",
  "version": "1.0.0",
  "description": "Browser tests for Meridian Components inventory dashboard",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
